}
if (rerun_preprocessing) {
DefaultAssay(seu) <- "RNA"
seu <- NormalizeData(seu, verbose = FALSE)
seu <- FindVariableFeatures(seu, nfeatures = 3000, verbose = FALSE)
seu <- ScaleData(seu, verbose = FALSE)
seu <- RunPCA(seu, npcs = 30, verbose = FALSE)
seu <- FindNeighbors(seu, dims = dims_use, verbose = FALSE)
seu <- FindClusters(seu, resolution = 0.4, verbose = FALSE)
seu <- RunUMAP(seu, dims = dims_use, verbose = FALSE)
}
sce <- as.SingleCellExperiment(seu)
# 写入 UMAP
reducedDims(sce)$UMAP <- Embeddings(seu, "umap")
# 如果有 PCA，也写入
if ("pca" %in% Reductions(seu)) {
reducedDims(sce)$PCA <- Embeddings(seu, "pca")
}
# 设置 Slingshot cluster label
colData(sce)$cluster_for_slingshot <- factor(seu@meta.data[[cluster_col]])
# 检查 root 是否存在
clus_levels <- levels(colData(sce)$cluster_for_slingshot)
print(clus_levels)
if (!root_cluster %in% clus_levels) {
stop(
paste0(
"root_cluster = ", root_cluster,
" 不在 cluster_for_slingshot 中。请检查 cluster_col 或 root_cluster 名字。"
)
)
}
sce <- slingshot(
sce,
clusterLabels = "cluster_for_slingshot",
reducedDim = slingshot_reduction,
start.clus = root_cluster
)
# 查看 lineage
print(slingLineages(sce))
pt <- slingPseudotime(sce, na = FALSE)
cw <- slingCurveWeights(sce)
pt_df <- as.data.frame(pt)
cw_df <- as.data.frame(cw)
colnames(pt_df) <- paste0("pt_lineage_", seq_len(ncol(pt_df)))
colnames(cw_df) <- paste0("cw_lineage_", seq_len(ncol(cw_df)))
meta_df <- seu@meta.data %>%
rownames_to_column("cell")
traj_df <- bind_cols(meta_df, pt_df, cw_df)
# 分配主 lineage
cw_mat <- as.matrix(cw_df)
traj_df$assigned_lineage <- apply(cw_mat, 1, function(x) {
if (all(is.na(x))) return(NA_character_)
paste0("Lineage_", which.max(x))
})
# 分配主 pseudotime
pt_mat <- as.matrix(pt_df)
traj_df$assigned_pseudotime <- sapply(seq_len(nrow(traj_df)), function(i) {
lin <- traj_df$assigned_lineage[i]
if (is.na(lin)) return(NA_real_)
idx <- as.numeric(gsub("Lineage_", "", lin))
pt_mat[i, idx]
})
# 写回 Seurat
seu$assigned_lineage <- traj_df$assigned_lineage[match(colnames(seu), traj_df$cell)]
seu$assigned_pseudotime <- traj_df$assigned_pseudotime[match(colnames(seu), traj_df$cell)]
# 保存细胞级结果
write.csv(
traj_df,
file = file.path(outdir, "cell_level_slingshot_results.csv"),
row.names = FALSE
)
# =========================
# 构建绘图数据：稳健版本
# =========================
umap_mat <- reducedDims(sce)$UMAP
# 检查 cluster_col 和 label_col 是否存在
if (!cluster_col %in% colnames(seu@meta.data)) {
stop(paste0("cluster_col = ", cluster_col, " 不在 seu@meta.data 中。"))
}
if (!label_col %in% colnames(seu@meta.data)) {
stop(paste0("label_col = ", label_col, " 不在 seu@meta.data 中。"))
}
# 先单独构建 metadata，明确生成 cluster_label 和 plot_label
meta_plot_df <- seu@meta.data %>%
rownames_to_column("cell") %>%
mutate(
cluster_label = as.character(.data[[cluster_col]]),
plot_label = as.character(.data[[label_col]])
) %>%
select(
cell,
cluster_label,
plot_label,
assigned_pseudotime,
assigned_lineage
)
# 构建 UMAP 坐标表
umap_plot_df <- data.frame(
cell = rownames(umap_mat),
UMAP_1 = umap_mat[, 1],
UMAP_2 = umap_mat[, 2],
stringsAsFactors = FALSE
)
# 合并
plot_df <- umap_plot_df %>%
left_join(meta_plot_df, by = "cell")
# 类型转换
plot_df$cluster_label <- as.factor(plot_df$cluster_label)
plot_df$plot_label <- as.factor(plot_df$plot_label)
plot_df$assigned_pseudotime <- as.numeric(plot_df$assigned_pseudotime)
# 检查
head(plot_df)
colnames(plot_df)
table(plot_df$plot_label, useNA = "ifany")
table(plot_df$cluster_label, useNA = "ifany")
colnames(plot_df)
nrow(plot_df)
length(plot_df$plot_label)
head(plot_df$plot_label)
label_df <- plot_df %>%
filter(!is.na(plot_label)) %>%
group_by(plot_label) %>%
summarise(
UMAP_1 = median(UMAP_1, na.rm = TRUE),
UMAP_2 = median(UMAP_2, na.rm = TRUE),
.groups = "drop"
)
head(label_df)
extract_clean_slingshot_curves <- function(
sce_obj,
n_bins = 100,
max_points_per_lineage = 150
) {
curves <- slingCurves(sce_obj)
if (length(curves) == 0) {
stop("没有检测到 Slingshot curves。请确认已经运行过 slingshot。")
}
curve_list <- lapply(seq_along(curves), function(i) {
crv <- curves[[i]]
# 提取曲线坐标
s <- as.data.frame(crv$s)
if (ncol(s) < 2) {
stop("曲线坐标少于2维，无法用于 UMAP 绘图。")
}
s <- s[, 1:2, drop = FALSE]
colnames(s) <- c("UMAP_1", "UMAP_2")
# 去掉异常值
s <- s %>%
filter(
is.finite(UMAP_1),
is.finite(UMAP_2)
)
if (nrow(s) < 2) {
warning(paste0("Lineage_", i, " 的曲线点少于2个，跳过。"))
return(NULL)
}
# 如果 crv$ord 长度和曲线点数量一致，使用 crv$ord 排序
if (!is.null(crv$ord) && length(crv$ord) == nrow(s)) {
s <- s[crv$ord, , drop = FALSE]
}
# 如果 crv$lambda 长度和曲线点数量一致，才使用 lambda 排序
# 如果不一致，说明 lambda 是细胞级别的，不用于曲线点排序
if (!is.null(crv$lambda) && length(crv$lambda) == nrow(s)) {
s$curve_order_value <- crv$lambda
s <- s %>% arrange(curve_order_value)
} else {
s$curve_order_value <- seq_len(nrow(s))
}
# 去除连续重复点
s <- s %>%
mutate(
dx = UMAP_1 - lag(UMAP_1),
dy = UMAP_2 - lag(UMAP_2),
dist_prev = sqrt(dx^2 + dy^2)
) %>%
filter(is.na(dist_prev) | dist_prev > 1e-8) %>%
select(-dx, -dy, -dist_prev)
if (nrow(s) < 2) {
warning(paste0("Lineage_", i, " 去重后曲线点少于2个，跳过。"))
return(NULL)
}
# 如果点太多，进行均匀抽样，避免线太密
n_keep <- min(max_points_per_lineage, nrow(s))
keep_idx <- unique(round(seq(1, nrow(s), length.out = n_keep)))
s <- s[keep_idx, , drop = FALSE]
s$Lineage <- paste0("Lineage_", i)
s$order <- seq_len(nrow(s))
s
})
curve_df <- bind_rows(curve_list)
if (is.null(curve_df) || nrow(curve_df) == 0) {
stop("没有可绘制的 Slingshot 曲线。")
}
curve_df <- curve_df %>%
arrange(Lineage, order)
return(curve_df)
}
curve_df_clean <- extract_clean_slingshot_curves(
sce,
n_bins = 100,
max_points_per_lineage = 150
)
head(curve_df_clean)
table(curve_df_clean$Lineage)
arrow_df <- curve_df_clean %>%
group_by(Lineage) %>%
arrange(order, .by_group = TRUE) %>%
summarise(
x = UMAP_1[max(1, floor(n() * 0.88))],
y = UMAP_2[max(1, floor(n() * 0.88))],
xend = UMAP_1[n()],
yend = UMAP_2[n()],
.groups = "drop"
)
endpoint_df <- curve_df_clean %>%
group_by(Lineage) %>%
arrange(order, .by_group = TRUE) %>%
summarise(
start_x = UMAP_1[1],
start_y = UMAP_2[1],
end_x = UMAP_1[n()],
end_y = UMAP_2[n()],
.groups = "drop"
)
arrow_df
endpoint_df
#标签位置
label_df <- plot_df %>%
filter(!is.na(plot_label)) %>%
group_by(plot_label) %>%
summarise(
UMAP_1 = median(UMAP_1, na.rm = TRUE),
UMAP_2 = median(UMAP_2, na.rm = TRUE),
.groups = "drop"
)
p_check_curve <- ggplot() +
geom_point(
data = plot_df,
aes(x = UMAP_1, y = UMAP_2),
color = "grey85",
size = 0.45,
alpha = 0.7
) +
geom_path(
data = curve_df_clean,
aes(x = UMAP_1, y = UMAP_2, group = Lineage),
color = "black",
linewidth = 0.7,
alpha = 0.85
) +
geom_segment(
data = arrow_df,
aes(x = x, y = y, xend = xend, yend = yend),
arrow = arrow(length = unit(0.18, "cm"), type = "closed"),
color = "black",
linewidth = 0.75,
alpha = 0.9
) +
coord_equal() +
theme_bw(base_size = 12) +
theme(panel.grid = element_blank()) +
labs(
title = "检查图：Slingshot 曲线和箭头",
x = "UMAP 1",
y = "UMAP 2"
)
p_check_curve
p_pseudotime_arrow <- ggplot(
plot_df,
aes(x = UMAP_1, y = UMAP_2, color = assigned_pseudotime)
) +
geom_point(size = 0.6, alpha = 0.9) +
scale_color_viridis_c(
option = "cividis",
na.value = "grey85",
name = "Pseudotime"
) +
geom_path(
data = curve_df_clean,
aes(x = UMAP_1, y = UMAP_2, group = Lineage),
inherit.aes = FALSE,
color = "black",
linewidth = 0.7,
alpha = 0.85
) +
geom_segment(
data = arrow_df,
aes(x = x, y = y, xend = xend, yend = yend),
inherit.aes = FALSE,
arrow = arrow(length = unit(0.2, "cm"), type = "closed"),
color = "black",
linewidth = 0.75,
alpha = 0.9
) +
geom_point(
data = endpoint_df,
aes(x = start_x, y = start_y),
inherit.aes = FALSE,
color = "#0072B2",
size = 2.8
) +
geom_point(
data = endpoint_df,
aes(x = end_x, y = end_y),
inherit.aes = FALSE,
color = "#D55E00",
size = 2.8
) +
geom_text_repel(
data = endpoint_df,
aes(x = start_x, y = start_y, label = paste0(Lineage, " start")),
inherit.aes = FALSE,
color = "#0072B2",
size = 3.2,
fontface = "bold",
max.overlaps = Inf
) +
geom_text_repel(
data = endpoint_df,
aes(x = end_x, y = end_y, label = paste0(Lineage, " end")),
inherit.aes = FALSE,
color = "#D55E00",
size = 3.2,
fontface = "bold",
max.overlaps = Inf
) +
coord_equal() +
theme_bw(base_size = 12) +
theme(
panel.grid = element_blank(),
plot.title = element_text(face = "bold", hjust = 0.5),
legend.position = "right"
) +
labs(
title = "Slingshot trajectory：pseudotime with directions",
x = "UMAP 1",
y = "UMAP 2"
)
p_pseudotime_arrow
p_pseudotime_arrow_label <- p_pseudotime_arrow +
geom_text_repel(
data = label_df,
aes(x = UMAP_1, y = UMAP_2, label = plot_label),
inherit.aes = FALSE,
color = "black",
size = 3.5,
fontface = "bold",
box.padding = 0.5,
point.padding = 0.3,
max.overlaps = Inf
)+
labs(
title = "Slingshot trajectory：pseudotime、directions with cell type annotations"
)
p_pseudotime_arrow_label
ggsave(
filename = file.path(outdir, "03_slingshot_pseudotime_arrow_labels.pdf"),
plot = p_pseudotime_arrow_label,
width = 10,
height = 7
)
ggsave(
filename = file.path(outdir, "03_slingshot_pseudotime_arrow_labels.png"),
plot = p_pseudotime_arrow_label,
width = 10,
height = 7,
dpi = 300
)
okabe_ito <- c(
"#E69F00",
"#56B4E9",
"#009E73",
"#F0E442",
"#0072B2",
"#D55E00",
"#CC79A7",
"#999999"
)
label_levels <- levels(plot_df$plot_label)
label_colors <- setNames(
colorRampPalette(okabe_ito)(length(label_levels)),
label_levels
)
p_celltype_arrow <- ggplot(
plot_df,
aes(x = UMAP_1, y = UMAP_2, color = plot_label)
) +
geom_point(size = 0.6, alpha = 0.85) +
scale_color_manual(
values = label_colors,
na.value = "grey85",
name = label_col
) +
geom_path(
data = curve_df_clean,
aes(x = UMAP_1, y = UMAP_2, group = Lineage),
inherit.aes = FALSE,
color = "black",
linewidth = 0.8,
alpha = 0.85
) +
geom_segment(
data = arrow_df,
aes(x = x, y = y, xend = xend, yend = yend),
inherit.aes = FALSE,
arrow = arrow(length = unit(0.2, "cm"), type = "closed"),
color = "black",
linewidth = 0.85,
alpha = 0.9
) +
geom_text_repel(
data = label_df,
aes(x = UMAP_1, y = UMAP_2, label = plot_label),
inherit.aes = FALSE,
color = "black",
size = 3.5,
fontface = "bold",
box.padding = 0.5,
point.padding = 0.3,
max.overlaps = Inf
)+
geom_point(
data = endpoint_df,
aes(x = start_x, y = start_y),
inherit.aes = FALSE,
color = "#0072B2",
size = 2.8
) +
geom_point(
data = endpoint_df,
aes(x = end_x, y = end_y),
inherit.aes = FALSE,
color = "#D55E00",
size = 2.8
) +
geom_text_repel(
data = endpoint_df,
aes(x = start_x, y = start_y, label = paste0(Lineage, " start")),
inherit.aes = FALSE,
color = "#0072B2",
size = 3.2,
fontface = "bold",
max.overlaps = Inf
) +
geom_text_repel(
data = endpoint_df,
aes(x = end_x, y = end_y, label = paste0(Lineage, " end")),
inherit.aes = FALSE,
color = "#D55E00",
size = 3.2,
fontface = "bold",
max.overlaps = Inf
) +
coord_equal() +
theme_bw(base_size = 12) +
theme(
panel.grid = element_blank(),
plot.title = element_text(face = "bold", hjust = 0.5),
legend.position = "right"
) +
labs(
title = "Slingshot trajectory: pseudotime with directions",
x = "UMAP 1",
y = "UMAP 2"
)
p_celltype_arrow
ggsave(
filename = file.path(outdir, "04_slingshot_celltype_arrow.pdf"),
plot = p_celltype_arrow,
width = 10,
height = 7
)
ggsave(
filename = file.path(outdir, "04_slingshot_celltype_arrow.png"),
plot = p_celltype_arrow,
width = 10,
height = 7,
dpi = 300
)
combined_plot <- p_celltype_arrow | p_pseudotime_arrow_label
ggsave(
filename = file.path(outdir, "05_combined_celltype_pseudotime_arrow.pdf"),
plot = combined_plot,
width = 16,
height = 7
)
ggsave(
filename = file.path(outdir, "05_combined_celltype_pseudotime_arrow.png"),
plot = combined_plot,
width = 16,
height = 7,
dpi = 300
)
saveRDS(seu, file = file.path(outdir, "seurat_with_slingshot_results.rds"))
saveRDS(sce, file = file.path(outdir, "sce_slingshot_umap.rds"))
saveRDS(curve_df_clean, file = file.path(outdir, "clean_slingshot_curve_df.rds"))
reticulate::repl_python()
setwd("/localdisk/home/s2845297/dissertation/trajectory")
Sys.setenv(RETICULATE_PYTHON = path.expand("~/conda_envs/scanpy_paga/bin/python"))
library(reticulate)
py_config()
library(reticulate)
py_config()
reticulate::repl_python()
Sys.setenv(RETICULATE_PYTHON = "/localdisk/home/s2845297/conda_envs/scanpy_paga/bin/python")
library(reticulate)
use_python("/localdisk/home/s2845297/conda_envs/scanpy_paga/bin/python", required = TRUE)
Sys.setenv(RETICULATE_PYTHON = "/localdisk/home/s2845297/conda_envs/scanpy_paga/bin/python")
library(reticulate)
use_python("/localdisk/home/s2845297/conda_envs/scanpy_paga/bin/python", required = TRUE)
py_config()
reticulate::repl_python()
